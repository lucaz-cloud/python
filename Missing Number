class Solution:
    def missingNumber(self, nums: List[int]) -> int:
        n = len(nums)
        total = (n * (n + 1)) // 2
        current_sum = 0

        for num in nums:
            current_sum += num

        return total - current_sum
